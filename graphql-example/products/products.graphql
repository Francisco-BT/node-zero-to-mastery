type Query {
  products: [Product]
  productsByPrice(minPrice: Float!, maxPrice: Float!): [Product]
  product(id: ID!): Product
}

type Product {
  description: ID!
  price: Float!
  reviews: [Review]
}

type Review {
  rating: Int!
  comment: String
}
